Title line

{{c1::Main idea::hint}} is important.

Inline `code` and **bold**.

```typescript
type Item = {
  id: number;
  label: "{{c2::name::label}}";
};

const items: Item[] = [{ id: 1, label: "one" }];
```
